extends ../layout/common.pug
block content
    h1 New Device

    if errors
        each error in errors
            .alert.alert-danger=error

    form(action="/users/create",class="d-flex" method="POST")
        .form-group
            label(for="name") Name
            input#name.form-control(type="text", name="name", value = values ? values.name : '')
        
        .form-group 
            lablel(for="imei") Imei
            input#imei.form-control(type="text", name="imei", value = values ? values.imei : '')
            
        button.btn.btn-primary Create
